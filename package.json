{
  "scripts": {
    "build": "turbo build && sh copy-dist.sh",
    "test": "turbo test",
    "dev": "node dev.js"
  },
  "engines": {
    "npm": "please use pnpm instead of npm",
    "yarn": "please use pnpm instead of yarn",
    "pnpm": ">=8.0.0",
    "node": ">=16.0.0"
  },
  "dependencies": {
    "turbo": "^1.9.7"
  },
  "devDependencies": {
    "node-option": "^0.0.6"
  }
}
